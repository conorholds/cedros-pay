/**
 * Standalone admin dashboard entrypoint.
 *
 * Use this entrypoint when rendering the full Cedros Pay admin dashboard
 * without the shared AdminShell plugin composition flow.
 */

export {
  CedrosPayAdminDashboard,
  type CedrosPayAdminDashboardProps,
  type DashboardSection,
} from './components/admin';
